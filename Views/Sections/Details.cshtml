@model LabbTsb.Models.Section

@{
    ViewData["Title"] = "Details";
}


<div>
    <h4 class="rubrik">@Model.Department</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Avdelning:
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Department)
        </dd>
       
    </dl>
</div>
<div>
    <a class="link" asp-action="Edit" asp-route-id="@Model.SectionId">Ändra</a> |
    <a class="link" asp-action="Index">Tillbaka till listan</a>
</div>
